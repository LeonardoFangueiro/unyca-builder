---
- name: Print blueprint_meta and host vars (databases)
  ansible.builtin.debug:
    msg:
      blueprint_meta: "{{ blueprint_meta | default({}) }}"
      host_vars: "{{ hostvars[inventory_hostname]['vars'] | default({}) }}"